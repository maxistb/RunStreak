//
//  AppColor.swift
//  RunStreak
//
//  Created by Maximillian Stabe on 07.10.25.
//

import SwiftUI

enum AppColor {
  static let background = Color(hex: "#FFF9F3")
  static let card = Color.white
  static let textPrimary = Color(hex: "#1E1E1E")
  static let textSecondary = Color(hex: "#6E6E73")

  // Existing Accents
  static let accentPeach = Color(hex: "#FFD1B5")
  static let accentLilac = Color(hex: "#E5D0FF")
  static let accentMint = Color(hex: "#BFF0D6")
  static let accentBlue = Color(hex: "#CFE8FF")

  // 🆕 New Accents
  static let accentPink = Color(hex: "#FFB3C6")   // soft pastel pink
  static let accentRed = Color(hex: "#FF7A7A")    // vibrant coral red
  static let accentPurple = Color(hex: "#CDB4FF") // gentle pastel violet

  // Utility & semantic
  static let success = Color(hex: "#32D74B")
  static let warning = Color(hex: "#FF9F0A")
  static let danger = Color(hex: "#FF453A")
}
